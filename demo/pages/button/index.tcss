.title {
  font-size: 18px;
  line-height: 24px;
  padding: 5px;
  margin-left: 8px;
  text-align: left;
  background: #fff;
}
.button--item {
  padding: 5px;
  flex-grow: 1;
}

.button--container {
  display: flex;
  flex-wrap: wrap;
  flex-direction: row;
  background: #fff;
}
